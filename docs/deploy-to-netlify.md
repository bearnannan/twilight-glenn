# วิธี Deploy ขึ้น Netlify (แบบง่ายที่สุด)

จากรูปภาพที่คุณส่งมา เราจะใช้วิธี **Drag & Drop** (ลากวาง) ซึ่งง่ายและรวดเร็วสำหรับโปรเจกต์นี้ครับ

## ขั้นตอนที่ 1: สร้างไฟล์สำหรับ Deploy (Build)
1. กลับมาที่ VS Code
2. เปิด Terminal แล้วพิมพ์คำสั่ง:
   ```bash
   npm run build
   ```
3. รอจนเสร็จ... คุณจะได้โฟลเดอร์ชื่อ `out` ขึ้นมาในโปรเจกต์

## ขั้นตอนที่ 2: ลากวางขึ้น Netlify
1. กลับไปที่หน้าเว็บ Netlify (ตามรูปที่คุณส่งมา)
2. เลื่อนลงมาข้างล่างสุด จะเห็นก้อนเมฆและเขียนว่า **"Drag and drop your project folder here"**
3. ลากโฟลเดอร์ `out` (ที่ได้จากข้อ 1) ใส่ลงไปในกรอบนั้นเลย!

## ขั้นตอนที่ 3: ตั้งค่า (สำคัญมาก)
เมื่อ Deploy เสร็จ เว็บจะยังใช้แผนที่ไม่ได้ เพราะเราต้องไปใส่ Key ก่อน
1. คลิกที่โปรเจกต์ที่เพิ่งสร้าง
2. ไปที่ **Site configuration** (เมนูด้านซ้าย)
3. เลือก **Environment variables**
4. กดปุ่ม **Add a variable**
   - Key: `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`
   - Value: (ใส่ Key เดียวกับที่คุณใส่ใน .env.local)
5. กด **Create variable**

## ขั้นตอนที่ 4: Domain Restriction (Google Cloud)
*เนื่องจากเว็บจริงจะไม่ใช่ localhost:3000 แล้ว*
1. ก๊อปปี้ Link เว็บไซต์ของคุณ (เช่น `https://fancy-site-12345.netlify.app`)
2. กลับไปที่ Google Cloud Console (หน้า API Key)
3. ตรง **Website restriction** ให้เพิ่ม URL ของ Netlify เข้าไป
   ```
   https://fancy-site-12345.netlify.app/*
   ```
4. กด Save

เสร็จเรียบร้อย! เว็บคุณออนไลน์แล้วครับ 🚀
